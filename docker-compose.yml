services:
  web:
    build: ./apps
    command: python manage.py runserver 0.0.0.0:8000
    volumes:
      - ./apps/:./apps
    ports:
      - 8000:8000
    env_file:
      - ./.env
    depends_on:
      - postgres
  postgres:
